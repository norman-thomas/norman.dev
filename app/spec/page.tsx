import { SpecSheet } from "@/components/SpecSheet";

// Archived view of the (live) Spec Sheet, with the design-lab switcher.
export default function SpecArchivePage() {
  return <SpecSheet showSwitcher />;
}
